---
phase: 01-runtime-audit-and-abstraction-plan
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/runtime/runtime-coupling-audit.md
autonomous: true
requirements:
  - RT-03
user_setup: []
must_haves:
  truths:
    - The repository has one source-of-truth audit that lists every current Claude-specific install root, skill registration path, CLI invocation point, scheduler hook, duplicated wrapper, and user-facing runtime string.
    - The audit names the exact files each follow-on compatibility phase must revisit instead of leaving ownership implicit.
  artifacts:
    - docs/runtime/runtime-coupling-audit.md
  key_links:
    - The audit cross-references install, skill, helper-script, server, UI, and README surfaces so later phases do not need to rediscover runtime coupling.
---

<objective>
Create the runtime coupling audit that later phases will use as the compatibility source of truth.

Purpose: Prevent Phase 2 and Phase 3 from rediscovering Claude-only assumptions by hand.
Output: `docs/runtime/runtime-coupling-audit.md` with layer-by-layer findings, duplicate-wrapper notes, and follow-on phase ownership.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-audit-and-abstraction-plan/01-CONTEXT.md
@.planning/phases/01-runtime-audit-and-abstraction-plan/01-RESEARCH.md
@.planning/phases/01-runtime-audit-and-abstraction-plan/01-PATTERNS.md
@install.sh
@README.md
@skill/SKILL.md
@skill/bin/start.sh
@skill/bin/install-schedule.sh
@skill/bin/run-daily-intel.sh
@skill/bin/generate-plan.sh
@skill/bin/code-review.sh
@skill/bin/uninstall.sh
@skill/bin/reinstall.sh
@app/scripts/generate-plan.sh
@app/scripts/code-review.sh
@app/server.js
@app/public/index.html
</context>

<threat_model>
## Trust Boundaries
- Source files to audit doc: every finding must remain traceable to a committed repo file so later phases do not act on assumptions.
- Audit doc to follow-on execution: omitted or miscategorized runtime surfaces can cause Codex gaps or Claude regressions in later implementation phases.

## Threat Register
- threat_id: T-01-01
  category: Tampering
  component: runtime audit coverage
  disposition: mitigate
  mitigation_plan: Require exact file references and grep-verifiable coverage for install, skill, scheduler, wrapper, server, UI, and docs surfaces.
- threat_id: T-01-02
  category: Repudiation
  component: follow-on phase handoff
  disposition: mitigate
  mitigation_plan: Record the target follow-on phase and the concrete impact for every audited finding so future changes stay justified and reviewable.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the audit artifact and required section structure</name>
  <files>docs/runtime/runtime-coupling-audit.md</files>
  <read_first>.planning/phases/01-runtime-audit-and-abstraction-plan/01-CONTEXT.md, .planning/phases/01-runtime-audit-and-abstraction-plan/01-RESEARCH.md, .planning/phases/01-runtime-audit-and-abstraction-plan/01-PATTERNS.md, install.sh, README.md, skill/SKILL.md</read_first>
  <action>Create `docs/runtime/runtime-coupling-audit.md`. Add the headings `# Runtime Coupling Audit`, `## Audit Scope`, `## Runtime Coupling Inventory`, `## Duplicate Wrapper Surface`, `## Follow-on Phase Impact`, and `## Verification Checklist`. In `## Runtime Coupling Inventory`, create a markdown table with the exact columns `Layer`, `File`, `Claude-specific assumption`, `Why it blocks Codex`, and `Target follow-on phase`. Seed rows for `install.sh`, `README.md`, `skill/SKILL.md`, `skill/bin/start.sh`, `skill/bin/install-schedule.sh`, `skill/bin/run-daily-intel.sh`, `skill/bin/generate-plan.sh`, `skill/bin/code-review.sh`, `skill/bin/uninstall.sh`, `skill/bin/reinstall.sh`, `app/scripts/generate-plan.sh`, `app/scripts/code-review.sh`, `app/server.js`, and `app/public/index.html`.</action>
  <verify>rg -n "Runtime Coupling Audit|Audit Scope|Runtime Coupling Inventory|Duplicate Wrapper Surface|Follow-on Phase Impact|Verification Checklist" docs/runtime/runtime-coupling-audit.md</verify>
  <acceptance_criteria>
    - `docs/runtime/runtime-coupling-audit.md contains "## Runtime Coupling Inventory"`
    - `docs/runtime/runtime-coupling-audit.md contains "install.sh"`
    - `docs/runtime/runtime-coupling-audit.md contains "app/public/index.html"`
  </acceptance_criteria>
  <done>The audit file exists with all required section headings and seeded rows for every known runtime-coupled source file.</done>
</task>

<task type="auto">
  <name>Task 2: Populate the audit with exact runtime assumptions and duplicate wrappers</name>
  <files>docs/runtime/runtime-coupling-audit.md</files>
  <read_first>docs/runtime/runtime-coupling-audit.md, install.sh, README.md, skill/SKILL.md, skill/bin/start.sh, skill/bin/install-schedule.sh, skill/bin/run-daily-intel.sh, skill/bin/generate-plan.sh, skill/bin/code-review.sh, skill/bin/uninstall.sh, skill/bin/reinstall.sh, app/scripts/generate-plan.sh, app/scripts/code-review.sh, app/server.js, app/public/index.html</read_first>
  <action>Fill the inventory rows with the literal Claude-coupled values currently in the repo: `~/.claude/job-quest`, `~/.claude/skills/job-quest/SKILL.md`, `claude`, `npx -y @anthropic-ai/claude-code`, `/job-quest`, `com.sidequest.job-quest.daily-intel`, `claude_prompt_`, and user-facing `Claude` / `Claude CLI` wording. In `## Duplicate Wrapper Surface`, document the exact duplicated pairs `skill/bin/generate-plan.sh` <-> `app/scripts/generate-plan.sh` and `skill/bin/code-review.sh` <-> `app/scripts/code-review.sh`, plus the overlap between `skill/bin/start.sh` and `app/start.sh`.</action>
  <verify>rg -n "~/.claude/job-quest|~/.claude/skills/job-quest/SKILL.md|@anthropic-ai/claude-code|com.sidequest.job-quest.daily-intel|claude_prompt_|Duplicate Wrapper Surface" docs/runtime/runtime-coupling-audit.md</verify>
  <acceptance_criteria>
    - `docs/runtime/runtime-coupling-audit.md contains "~/.claude/job-quest"`
    - `docs/runtime/runtime-coupling-audit.md contains "@anthropic-ai/claude-code"`
    - `docs/runtime/runtime-coupling-audit.md contains "skill/bin/generate-plan.sh"`
    - `docs/runtime/runtime-coupling-audit.md contains "app/scripts/code-review.sh"`
  </acceptance_criteria>
  <done>The audit explicitly documents the current Claude-only literals and the duplicated wrapper surfaces that later phases must consolidate.</done>
</task>

<task type="auto">
  <name>Task 3: Add phase ownership and a grep-verifiable coverage checklist</name>
  <files>docs/runtime/runtime-coupling-audit.md</files>
  <read_first>docs/runtime/runtime-coupling-audit.md, .planning/ROADMAP.md, .planning/phases/01-runtime-audit-and-abstraction-plan/01-RESEARCH.md</read_first>
  <action>Add `## Follow-on Phase Impact` with the exact subsections `### Phase 2 install and registration`, `### Phase 3 runtime execution`, `### Phase 4 hardening and UX`, and `### Phase 5 docs and validation`. Under each subsection, list the exact files from the inventory that phase must revisit. Add `## Verification Checklist` with one bullet per audited file so a later reviewer can prove coverage with `rg` instead of rereading the whole phase discussion.</action>
  <verify>rg -n "Phase 2 install and registration|Phase 3 runtime execution|Phase 4 hardening and UX|Phase 5 docs and validation|Verification Checklist|skill/bin/reinstall.sh|app/server.js|README.md" docs/runtime/runtime-coupling-audit.md</verify>
  <acceptance_criteria>
    - `docs/runtime/runtime-coupling-audit.md contains "### Phase 2 install and registration"`
    - `docs/runtime/runtime-coupling-audit.md contains "### Phase 3 runtime execution"`
    - `docs/runtime/runtime-coupling-audit.md contains "## Verification Checklist"`
    - `docs/runtime/runtime-coupling-audit.md contains "skill/bin/reinstall.sh"`
  </acceptance_criteria>
  <done>The audit tells later phases exactly which files they own and includes a coverage checklist that can be validated by grep.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n "install.sh|README.md|skill/SKILL.md|skill/bin/start.sh|skill/bin/install-schedule.sh|skill/bin/run-daily-intel.sh|app/server.js|app/public/index.html" docs/runtime/runtime-coupling-audit.md`
- [ ] `rg -n "~/.claude/job-quest|@anthropic-ai/claude-code|Duplicate Wrapper Surface|claude_prompt_" docs/runtime/runtime-coupling-audit.md`
- [ ] `rg -n "Phase 2 install and registration|Phase 3 runtime execution|Phase 4 hardening and UX|Phase 5 docs and validation" docs/runtime/runtime-coupling-audit.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `docs/runtime/runtime-coupling-audit.md` covers every currently known runtime-coupled source file
- Every audited finding names the follow-on phase that should consume it
  </success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-audit-and-abstraction-plan/01-01-SUMMARY.md`
</output>
