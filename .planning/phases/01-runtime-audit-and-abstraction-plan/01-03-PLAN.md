---
phase: 01-runtime-audit-and-abstraction-plan
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/runtime/runtime-coupling-audit.md
autonomous: true
gap_closure: true
requirements:
  - RT-03
user_setup: []
must_haves:
  truths:
    - Runtime-specific assumptions in scripts, prompts, docs, and skill assets are cataloged, including the Claude-coupled `app/README.md` surface.
    - The source-of-truth audit and verification checklist cover every currently visible Claude-coupled documentation surface needed by later phases.
  artifacts:
    - docs/runtime/runtime-coupling-audit.md
  key_links:
    - The audit inventory, follow-on phase impact, and verification checklist all name `app/README.md` so later phases do not need to rediscover that documentation seam.
---

<objective>
Close the remaining audit coverage gap in Phase 1 by adding the uncataloged `app/README.md` Claude-coupled surface to the runtime audit and downstream handoff.

Purpose: Make the Phase 1 audit exhaustive against the current repo-visible documentation surfaces.
Output: `docs/runtime/runtime-coupling-audit.md` updated to include `app/README.md` in the inventory, phase handoff, and verification checklist.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-audit-and-abstraction-plan/01-CONTEXT.md
@.planning/phases/01-runtime-audit-and-abstraction-plan/01-RESEARCH.md
@.planning/phases/01-runtime-audit-and-abstraction-plan/01-PATTERNS.md
@.planning/phases/01-runtime-audit-and-abstraction-plan/01-VERIFICATION.md
@.planning/phases/01-runtime-audit-and-abstraction-plan/01-01-SUMMARY.md
@docs/runtime/runtime-coupling-audit.md
@app/README.md
</context>

<threat_model>
## Trust Boundaries
- Runtime audit to later phases: if `app/README.md` stays uncataloged, later docs and UX work will miss a live Claude-only surface.
- Verification checklist to reviewers: incomplete grep coverage would allow the audit to claim repo-wide coverage without proving it.

## Threat Register
- threat_id: T-01-05
  category: Tampering
  component: runtime audit completeness
  disposition: mitigate
  mitigation_plan: Add `app/README.md` to the inventory, phase-impact section, and checklist with exact Claude-coupled strings from the file.
- threat_id: T-01-06
  category: Repudiation
  component: verification handoff
  disposition: mitigate
  mitigation_plan: Make the checklist and follow-on phase ownership mention `app/README.md` explicitly so later work can prove the surface was intentionally handed off.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add `app/README.md` to the runtime coupling inventory with exact Claude-coupled wording</name>
  <files>docs/runtime/runtime-coupling-audit.md</files>
  <read_first>docs/runtime/runtime-coupling-audit.md, app/README.md, .planning/phases/01-runtime-audit-and-abstraction-plan/01-VERIFICATION.md, .planning/phases/01-runtime-audit-and-abstraction-plan/01-01-SUMMARY.md</read_first>
  <action>Insert a new `Docs` inventory row for `app/README.md` in `docs/runtime/runtime-coupling-audit.md`. In the `Claude-specific assumption` column, cite the current file's exact runtime-coupled surfaces: `AI Code Review -- Get feedback on your solutions via the Claude CLI`, the optional `[Claude CLI](https://github.com/anthropics/claude-code)` prerequisite for code review and interview-plan generation, `scripts/           # Helper scripts (Claude CLI wrapper)`, and `POST /api/code-review | AI code review via Claude`. In the `Why it blocks Codex` column, state that this embedded app-level README would continue telling users and contributors that the app's AI features are Claude-only. Set `Target follow-on phase` to `Phase 5 docs and validation`.</action>
  <verify>rg -n '`app/README.md`|Claude CLI wrapper|AI code review via Claude|Phase 5 docs and validation' docs/runtime/runtime-coupling-audit.md</verify>
  <acceptance_criteria>
    - `docs/runtime/runtime-coupling-audit.md contains "\`app/README.md\`"`
    - `docs/runtime/runtime-coupling-audit.md contains "AI code review via Claude"`
    - `docs/runtime/runtime-coupling-audit.md contains "Claude CLI wrapper"`
    - `docs/runtime/runtime-coupling-audit.md contains "Phase 5 docs and validation"`
  </acceptance_criteria>
  <done>The audit inventory now covers the previously uncataloged Claude-coupled app README surface with exact evidence from the file.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the downstream handoff and verification checklist to prove the app README is covered</name>
  <files>docs/runtime/runtime-coupling-audit.md</files>
  <read_first>docs/runtime/runtime-coupling-audit.md, app/README.md, .planning/ROADMAP.md, .planning/phases/01-runtime-audit-and-abstraction-plan/01-VERIFICATION.md</read_first>
  <action>Update `## Follow-on Phase Impact` so `Phase 5 docs and validation` explicitly lists `app/README.md` alongside `README.md` and `skill/SKILL.md` as a documentation surface that must be rewritten for dual-runtime support. Update `## Verification Checklist` with a dedicated `app/README.md` bullet that greps for `Claude CLI`, `Claude CLI wrapper`, and `AI code review via Claude`. In the surrounding prose for the audit scope or checklist introduction, state that the checklist now covers every repo-visible Claude-coupled documentation surface, including the app-local README.</action>
  <verify>rg -n 'app/README.md|every repo-visible Claude-coupled documentation surface|Claude CLI wrapper|AI code review via Claude' docs/runtime/runtime-coupling-audit.md</verify>
  <acceptance_criteria>
    - `docs/runtime/runtime-coupling-audit.md contains "app/README.md"`
    - `docs/runtime/runtime-coupling-audit.md contains "every repo-visible Claude-coupled documentation surface"`
    - `docs/runtime/runtime-coupling-audit.md contains "Claude CLI"`
    - `docs/runtime/runtime-coupling-audit.md contains "AI code review via Claude"`
  </acceptance_criteria>
  <done>The phase handoff and verification checklist both prove that `app/README.md` is intentionally covered by the runtime audit.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n '\`app/README.md\`|Claude CLI wrapper|AI code review via Claude|Phase 5 docs and validation' docs/runtime/runtime-coupling-audit.md`
- [ ] `rg -n 'every repo-visible Claude-coupled documentation surface|Verification Checklist|app/README.md' docs/runtime/runtime-coupling-audit.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- `docs/runtime/runtime-coupling-audit.md` explicitly covers `app/README.md` as a current Claude-coupled documentation surface
- The Phase 5 handoff and audit checklist both mention `app/README.md` so later phases do not need to rediscover it
  </success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-audit-and-abstraction-plan/01-03-SUMMARY.md`
</output>
