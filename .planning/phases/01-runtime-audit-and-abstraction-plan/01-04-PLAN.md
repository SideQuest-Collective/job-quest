---
phase: 01-runtime-audit-and-abstraction-plan
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/runtime/runtime-contract.md
  - docs/runtime/runtime-config-example.json
  - docs/runtime/runtime-migration.md
autonomous: true
gap_closure: true
requirements:
  - RT-03
user_setup: []
must_haves:
  truths:
    - The runtime contract and migration strategy define one bootstrap activation order: detect the invoking runtime immediately, validate it, then set `activeRuntime`.
    - The runtime contract and migration strategy define one deferred-path rule: when `migrationState=deferred`, `productHomeDir` stays on the last-known-good root and `pendingCanonicalHomeDir` remains `~/.job-quest`.
    - The runtime descriptor documents persisted runtime-validation diagnostics so failed runtime switches do not require later phases to invent an undocumented schema field.
  artifacts:
    - docs/runtime/runtime-contract.md
    - docs/runtime/runtime-config-example.json
    - docs/runtime/runtime-migration.md
  key_links:
    - The contract, migration guide, and config example all use the same `runtimeValidation` field and the same activation/deferred-path semantics.
---

<objective>
Reconcile the Phase 1 runtime contract, migration strategy, and config example so they present one unambiguous bootstrap, deferred-migration, and diagnostics model.

Purpose: Remove the contradictory guidance that currently blocks later phases from treating Phase 1 as the single implementation source of truth.
Output: `docs/runtime/runtime-contract.md`, `docs/runtime/runtime-migration.md`, and `docs/runtime/runtime-config-example.json` updated to share the same activation order, deferred-path rule, and validation diagnostics field.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-runtime-audit-and-abstraction-plan/01-CONTEXT.md
@.planning/phases/01-runtime-audit-and-abstraction-plan/01-RESEARCH.md
@.planning/phases/01-runtime-audit-and-abstraction-plan/01-PATTERNS.md
@.planning/phases/01-runtime-audit-and-abstraction-plan/01-VERIFICATION.md
@.planning/phases/01-runtime-audit-and-abstraction-plan/01-02-SUMMARY.md
@docs/runtime/runtime-contract.md
@docs/runtime/runtime-migration.md
@docs/runtime/runtime-config-example.json
</context>

<threat_model>
## Trust Boundaries
- Contract docs to implementation phases: inconsistent bootstrap or deferred-path semantics would cause later install and runtime code to persist the wrong default or resolve the wrong product home.
- Runtime descriptor to operations: undocumented diagnostics fields force later phases to invent schema outside the approved contract.

## Threat Register
- threat_id: T-01-07
  category: Tampering
  component: runtime switch semantics
  disposition: mitigate
  mitigation_plan: Rewrite the contract and migration docs so they use the same ordered steps for `detectedRuntime`, validation, and `activeRuntime` persistence.
- threat_id: T-01-08
  category: Repudiation
  component: runtime descriptor schema
  disposition: mitigate
  mitigation_plan: Add one documented `runtimeValidation` object to the contract and config example so runtime-switch failures have an agreed persisted location.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile the runtime contract with the deferred-migration and validation rules</name>
  <files>docs/runtime/runtime-contract.md</files>
  <read_first>docs/runtime/runtime-contract.md, docs/runtime/runtime-migration.md, docs/runtime/runtime-config-example.json, .planning/phases/01-runtime-audit-and-abstraction-plan/01-VERIFICATION.md, .planning/phases/01-runtime-audit-and-abstraction-plan/01-02-SUMMARY.md</read_first>
  <action>Update `docs/runtime/runtime-contract.md` so the `Runtime Descriptor` table includes a top-level `runtimeValidation` object with the exact child keys `status`, `lastCheckedRuntime`, `lastSuccessAt`, `lastFailureAt`, and `lastFailureReason`. In `Normalization rules`, `Path Resolution Rules`, and `Runtime Switch Semantics`, make the contract match the verifier's required target state exactly: `detectedRuntime` is recorded immediately on bootstrap or invocation, `activeRuntime` is written only after the candidate runtime passes validation, `productHomeDir` resolves to `~/.job-quest` only when `migrationState=ready`, and `productHomeDir` remains on the last-known-good root while `migrationState=deferred` with `pendingCanonicalHomeDir=~/.job-quest`.</action>
  <verify>rg -n 'runtimeValidation|lastCheckedRuntime|lastFailureReason|activeRuntime.*only after|migrationState=deferred|last-known-good root|pendingCanonicalHomeDir=~/.job-quest' docs/runtime/runtime-contract.md</verify>
  <acceptance_criteria>
    - `docs/runtime/runtime-contract.md contains "runtimeValidation"`
    - `docs/runtime/runtime-contract.md contains "lastCheckedRuntime"`
    - `docs/runtime/runtime-contract.md contains "activeRuntime"`
    - `docs/runtime/runtime-contract.md contains "last-known-good root"`
    - `docs/runtime/runtime-contract.md contains "pendingCanonicalHomeDir=~/.job-quest"`
  </acceptance_criteria>
  <done>The contract now defines one diagnostics field and one consistent activation/deferred-path model.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the migration guide to use the same bootstrap and deferred-path semantics as the contract</name>
  <files>docs/runtime/runtime-migration.md</files>
  <read_first>docs/runtime/runtime-migration.md, docs/runtime/runtime-contract.md, .planning/phases/01-runtime-audit-and-abstraction-plan/01-VERIFICATION.md, .planning/phases/01-runtime-audit-and-abstraction-plan/01-02-SUMMARY.md</read_first>
  <action>Update `docs/runtime/runtime-migration.md` so `## Bootstrap Behavior`, `## Automatic Runtime Switch`, and the deferred-conflict rules use the same exact semantics as the contract: record `detectedRuntime` immediately, validate readiness before first setting or later switching `activeRuntime`, keep `productHomeDir` on the last-known-good root when `migrationState=deferred`, keep `pendingCanonicalHomeDir=~/.job-quest`, and record failed readiness checks in the documented `runtimeValidation` object instead of referring to unspecified diagnostics persistence.</action>
  <verify>rg -n 'detectedRuntime|activeRuntime|runtimeValidation|last-known-good root|pendingCanonicalHomeDir=~/.job-quest|readiness checks fail' docs/runtime/runtime-migration.md</verify>
  <acceptance_criteria>
    - `docs/runtime/runtime-migration.md contains "runtimeValidation"`
    - `docs/runtime/runtime-migration.md contains "detectedRuntime"`
    - `docs/runtime/runtime-migration.md contains "activeRuntime"`
    - `docs/runtime/runtime-migration.md contains "last-known-good root"`
    - `docs/runtime/runtime-migration.md contains "pendingCanonicalHomeDir=~/.job-quest"`
  </acceptance_criteria>
  <done>The migration guide no longer conflicts with the contract about activation order, deferred paths, or diagnostics persistence.</done>
</task>

<task type="auto">
  <name>Task 3: Extend the runtime config example with the documented validation diagnostics field</name>
  <files>docs/runtime/runtime-config-example.json</files>
  <read_first>docs/runtime/runtime-config-example.json, docs/runtime/runtime-contract.md, docs/runtime/runtime-migration.md, .planning/phases/01-runtime-audit-and-abstraction-plan/01-VERIFICATION.md</read_first>
  <action>Update `docs/runtime/runtime-config-example.json` to include the top-level `runtimeValidation` object with the exact keys `status`, `lastCheckedRuntime`, `lastSuccessAt`, `lastFailureAt`, and `lastFailureReason`. Keep the example on a `migrationState: "ready"` happy path, but ensure the rest of the object still uses the same canonical field names documented in the contract and referenced by the migration guide.</action>
  <verify>rg -n '"runtimeValidation"|"status"|"lastCheckedRuntime"|"lastSuccessAt"|"lastFailureAt"|"lastFailureReason"' docs/runtime/runtime-config-example.json</verify>
  <acceptance_criteria>
    - `docs/runtime/runtime-config-example.json contains "\"runtimeValidation\""`
    - `docs/runtime/runtime-config-example.json contains "\"lastCheckedRuntime\""`
    - `docs/runtime/runtime-config-example.json contains "\"lastFailureReason\""`
    - `docs/runtime/runtime-config-example.json contains "\"migrationState\": \"ready\""`
  </acceptance_criteria>
  <done>The config example now documents where runtime-validation diagnostics live without changing the agreed ready-state example.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rg -n 'runtimeValidation|lastCheckedRuntime|lastFailureReason|migrationState=deferred|last-known-good root|pendingCanonicalHomeDir=~/.job-quest' docs/runtime/runtime-contract.md`
- [ ] `rg -n 'runtimeValidation|detectedRuntime|activeRuntime|last-known-good root|pendingCanonicalHomeDir=~/.job-quest' docs/runtime/runtime-migration.md`
- [ ] `rg -n '"runtimeValidation"|"lastCheckedRuntime"|"lastFailureReason"|"migrationState": "ready"' docs/runtime/runtime-config-example.json`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The contract and migration guide use the same activation order for `detectedRuntime`, validation, and `activeRuntime`
- The contract and migration guide use the same deferred-path rule for `productHomeDir` and `pendingCanonicalHomeDir`
- `docs/runtime/runtime-config-example.json` documents the persisted runtime-validation diagnostics field required by the migration strategy
  </success_criteria>

<output>
After completion, create `.planning/phases/01-runtime-audit-and-abstraction-plan/01-04-SUMMARY.md`
</output>
