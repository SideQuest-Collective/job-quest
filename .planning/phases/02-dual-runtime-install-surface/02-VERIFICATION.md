---
status: passed
---

# Phase 2 Verification

- Shared install root moved to `~/.job-quest`
- Shared data moved to `~/.job-quest/data`
- Claude and Codex skill registrations are both written by the installer
- Lifecycle scripts target the shared home instead of a Claude-only root
