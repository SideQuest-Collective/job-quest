# 02-02 Summary

The installer now writes `SKILL.md` into both `~/.claude/skills/job-quest/` and `~/.codex/skills/job-quest/`, while the active runtime is persisted in `~/.job-quest/config/runtime.json`.
