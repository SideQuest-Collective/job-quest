# 02-01 Plan

- Refactor `install.sh` around the shared `~/.job-quest` layout.
- Seed `app/`, `data/`, `bin/`, `references/`, and `config/runtime.json`.
- Import legacy Claude-root data into the shared data tree.
