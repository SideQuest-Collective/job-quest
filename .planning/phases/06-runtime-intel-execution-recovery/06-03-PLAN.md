---
phase: 06-runtime-intel-execution-recovery
plan: 03
type: execute
wave: 2
depends_on:
  - "06-01"
  - "06-02"
files_modified:
  - app/package.json
  - app/tests/runtime-shell.test.js
  - app/tests/local-date.test.js
  - docs/validation/dual-runtime-checklist.md
autonomous: true
requirements:
  - RT-02
  - PROD-01
user_setup: []
must_haves:
  truths:
    - The repaired Codex runner path is protected by automated regression coverage, not just manual shell inspection.
    - The local-date helper behavior is covered by automated tests that prove "today" is based on the local calendar date and that fallback behavior is explicit.
    - The validation checklist tells a reviewer exactly how to verify the repaired Codex daily intel path and local-date behavior in an installed environment.
  artifacts:
    - app/tests/runtime-shell.test.js
    - app/tests/local-date.test.js
    - docs/validation/dual-runtime-checklist.md
  key_links:
    - The automated tests assert the exact command and helper behavior introduced by Plans 06-01 and 06-02, and the validation checklist mirrors those repaired paths.
---

<objective>
Add regression coverage and validation evidence for the repaired runtime and date-boundary behavior.

Purpose: Prevent Phase 6 from closing with only manual reasoning about the Codex helper path or dashboard date logic.
Output: automated tests plus validation guidance that prove the repaired paths in both repo-local and installed environments.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-runtime-intel-execution-recovery/06-RESEARCH.md
@lib/runtime-shell.sh
@skill/bin/run-daily-intel.sh
@app/lib/local-date.js
@app/server.js
@app/package.json
@docs/validation/dual-runtime-checklist.md
</context>

<threat_model>
## Trust Boundaries
- Implementation to verification: Phase 6 can appear complete while still regressing later if the repaired runtime command and local-date contract are not encoded in tests.
- Repo validation to installed validation: the written checklist must mirror the actual installed helper paths or release proof will drift from the product.

## Threat Register
- threat_id: T-06-05
  category: Repudiation
  component: regression coverage
  disposition: mitigate
  mitigation_plan: Add tests that assert the dry-run Codex command includes both required `--add-dir` flags and that the local-date helper prefers today's record before fallback.
- threat_id: T-06-06
  category: Integrity
  component: validation evidence
  disposition: mitigate
  mitigation_plan: Update the validation checklist with the exact installed `~/.job-quest/bin/run-daily-intel.sh` command and explicit local-date endpoint checks.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add automated tests for the repaired Codex shell path and local-date helper</name>
  <files>app/tests/runtime-shell.test.js, app/tests/local-date.test.js, app/package.json</files>
  <read_first>lib/runtime-shell.sh, skill/bin/run-daily-intel.sh, app/lib/local-date.js, app/package.json</read_first>
  <action>Create an `app/tests/` directory if it does not exist. Add `app/tests/runtime-shell.test.js` using Node's built-in `node:test` runner and `child_process.spawnSync` to execute `bash -lc` snippets that source `../lib/runtime-shell.sh`, set `JOB_QUEST_ACTIVE_RUNTIME=codex`, `JOB_QUEST_RUNTIME_COMMAND=codex`, `JOB_QUEST_RUNTIME_COMMAND_ARGS=(exec)`, `JOB_QUEST_APP_ROOT=/tmp/job-quest-app`, `JOB_QUEST_DATA_DIR=/tmp/job-quest-data`, `JOB_QUEST_REFERENCES_DIR=/tmp/job-quest-references`, and `JOB_QUEST_RUNTIME_DRY_RUN=1`, then assert the printed command includes `-C /tmp/job-quest-app`, `--add-dir /tmp/job-quest-data`, and `--add-dir /tmp/job-quest-references`. Add `app/tests/local-date.test.js` that imports `../lib/local-date` and asserts `getLocalDateStamp()` returns `YYYY-MM-DD` from local calendar components and `preferTodayOrLatest()` returns today's dated record before falling back to the first record. Update `app/package.json` so `npm test` runs `node --test tests/*.test.js` instead of the placeholder error script.</action>
  <verify>cd app && npm test</verify>
  <acceptance_criteria>
    - `app/package.json contains 'node --test tests/*.test.js'`
    - `app/tests/runtime-shell.test.js contains 'JOB_QUEST_REFERENCES_DIR=/tmp/job-quest-references'`
    - `app/tests/runtime-shell.test.js contains '--add-dir /tmp/job-quest-references'`
    - `app/tests/local-date.test.js contains 'preferTodayOrLatest'`
  </acceptance_criteria>
  <done>The repo has fast automated coverage for the repaired Codex command shape and the shared local-date helper behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Update the validation checklist with the repaired installed-flow proof</name>
  <files>docs/validation/dual-runtime-checklist.md</files>
  <read_first>docs/validation/dual-runtime-checklist.md, skill/bin/run-daily-intel.sh, app/server.js, .planning/ROADMAP.md</read_first>
  <action>Update `docs/validation/dual-runtime-checklist.md` so the helper-script section explicitly calls out Codex daily intel verification from `~/.job-quest/bin/run-daily-intel.sh` and expects fresh files under `~/.job-quest/data/intel/`, `~/.job-quest/data/quizzes/`, and `~/.job-quest/data/tasks/`. Add a new local-date verification subsection that tells reviewers to compare `/api/job-status`, `/api/intel/latest`, `/api/quizzes/today`, and `/api/tasks/today` against the same local-day artifact filenames and verify the endpoints remain consistent near local day boundaries.</action>
  <verify>rg -n '~/.job-quest/bin/run-daily-intel.sh|/api/job-status|/api/intel/latest|/api/quizzes/today|/api/tasks/today|local-date|day boundaries' docs/validation/dual-runtime-checklist.md</verify>
  <acceptance_criteria>
    - `docs/validation/dual-runtime-checklist.md contains '~/.job-quest/bin/run-daily-intel.sh'`
    - `docs/validation/dual-runtime-checklist.md contains '/api/intel/latest'`
    - `docs/validation/dual-runtime-checklist.md contains '/api/quizzes/today'`
    - `docs/validation/dual-runtime-checklist.md contains 'day boundaries'`
  </acceptance_criteria>
  <done>The validation checklist now proves the exact installed Codex helper path and the endpoint-level local-date contract that Phase 6 repaired.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd app && npm test`
- [ ] `rg -n '~/.job-quest/bin/run-daily-intel.sh|/api/job-status|/api/intel/latest|/api/quizzes/today|/api/tasks/today|day boundaries' docs/validation/dual-runtime-checklist.md`
- [ ] `rg -n 'JOB_QUEST_REFERENCES_DIR|--add-dir /tmp/job-quest-references|preferTodayOrLatest|getLocalDateStamp' app/tests/*.test.js`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repaired Codex runner command and the shared local-date helper are both covered by automated tests
- The validation checklist gives a future reviewer concrete installed-environment proof steps for the runtime and date-boundary fixes
  </success_criteria>

<output>
After completion, create `.planning/phases/06-runtime-intel-execution-recovery/06-03-SUMMARY.md`
</output>
