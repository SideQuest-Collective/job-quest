# 06-01 Summary

`lib/runtime-shell.sh` now exposes a dry-run Codex command shape that includes both shared data and references directories, and `run-daily-intel.sh` logs the resolved runtime/app/data/reference paths before execution.
